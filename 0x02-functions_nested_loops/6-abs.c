#include "main.h"

/**
 * _abs  - prints the absolute value of a number
 * @n: The number to be checked
 *
 * Return: Always 0 (Success)
 *
 */

int _abs(int n)
{
if (n < 0)
{
n = n * -1;
return (n);
}
else
{
return (n);
}
return (0);
}
